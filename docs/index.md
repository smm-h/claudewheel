---
description: "claudewheel is a TUI launcher for Claude Code. API reference covering configuration, profiles, rendering, terminal I/O, themes, and session launching."
---

# claudewheel

TUI launcher for Claude Code

## API Reference

The claudewheel API covers configuration, profile discovery, TUI rendering, terminal I/O, segment bar layout, theme parsing, and session launching. Modules are organized by concern: `config` and `defaults` handle persistent settings, `renderer` and `terminal` drive the display, `segment` defines the bar data model, `launch` builds the exec command, and `profile` resolves Claude Code profile directories. This reference is auto-generated from docstrings.

:-: ref path="claudewheel"
