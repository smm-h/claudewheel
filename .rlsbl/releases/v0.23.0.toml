# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "Launch-target client adapters: the new --client flag can launch alternative clients (miniclaude) with mapped profile/model/permission selections."
# Optional context explaining why these changes were made
context = "Maintenance items riding along with this release: strictcli dependency bumped to >=0.27.0, and the docs were migrated to schema auto-discovery."
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["npm", "pypi"]
exclude = []
