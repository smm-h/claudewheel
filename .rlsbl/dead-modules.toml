# Declared non-entry source units: files that are legitimately unreachable by
# the production import graph, together with the real reason each is exempt.

[[known_non_entry]]
path = ".stricttools/docs/_directives/guardrail_table.py"
reason = "selfdoc custom directive. Loaded by selfdoc via the 'directives' map in selfdoc.json (table-guardrails -> .stricttools/docs/_directives/guardrail_table.py), not by any Python import. It is invoked as a documentation directive at doc-generation time, so no module imports it -- it is an entry point for selfdoc, not dead code."

[[known_non_entry]]
path = ".stricttools/docs/_directives/disallowed_tools_table.py"
reason = "selfdoc custom directive. Loaded by selfdoc via the 'directives' map in selfdoc.json (table-disallowed-tools -> .stricttools/docs/_directives/disallowed_tools_table.py), not by any Python import. It is invoked as a documentation directive at doc-build time, so no production module imports it -- it is an entry point for selfdoc, not dead code."
